# Cluster-wide settings
cluster: {
    id: cluster1
    mode: gateway  # direct or gateway
    scheduler: {
        releasedOnUnlock: false
    }
}
group: {
    id: aspectow-demo-console
    title: Aspectow Demo Console
    description: This group is for Aspectow Demo.
}
